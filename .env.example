# ============================================
# LANGFUSE - Variaveis de Ambiente
# ============================================
# Copie este arquivo para .env e altere os valores
# Para producao, GERE NOVOS SECRETS!

# PostgreSQL
DB_PASSWORD=langfuse123

# ClickHouse
CLICKHOUSE_PASSWORD=clickhouse123

# Redis
REDIS_PASSWORD=redis123

# MinIO (S3)
MINIO_USER=langfuse
MINIO_PASSWORD=minio123456

# Auth - GERAR NOVOS EM PRODUCAO!
# Execute: openssl rand -base64 32
NEXTAUTH_SECRET=gerar-com-openssl-rand-base64-32
SALT=gerar-com-openssl-rand-base64-32

# Execute: openssl rand -hex 32
ENCRYPTION_KEY=gerar-com-openssl-rand-hex-32

# URL publica do Langfuse (alterar para seu dominio)
# NEXTAUTH_URL=https://langfuse.seudominio.com
